[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis"
version = "0.1.0"
description = "Shared library for AegisAML"
dependencies = [
    "pydantic>=2.0.0",
    "fastapi>=0.100.0"
]
