apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: aegis-aml
spec:
  type: ClusterIP
  selector:
    app: gateway-service
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
