apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: aegis-aml
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
