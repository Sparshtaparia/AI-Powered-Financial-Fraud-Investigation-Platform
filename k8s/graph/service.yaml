apiVersion: v1
kind: Service
metadata:
  name: graph-service
  namespace: aegis-aml
spec:
  type: ClusterIP
  selector:
    app: graph-service
  ports:
    - protocol: TCP
      port: 8001
      targetPort: 8001
